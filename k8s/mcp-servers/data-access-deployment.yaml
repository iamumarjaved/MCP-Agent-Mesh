---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-access-mcp
  namespace: agent-mesh
  labels:
    app: agent-mesh
    component: data-access-mcp
    tier: mcp-server
    version: "1.0.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-mesh
      component: data-access-mcp
  template:
    metadata:
      labels:
        app: agent-mesh
        component: data-access-mcp
        tier: mcp-server
        version: "1.0.0"
    spec:
      serviceAccountName: default
      containers:
        - name: data-access-mcp
          image: ${ACR_LOGIN_SERVER}/agent-mesh-data-access-mcp:latest
          ports:
            - containerPort: 9000
              name: http
              protocol: TCP
          envFrom:
            - configMapRef:
                name: agent-mesh-config
          env:
            - name: SERVICE_NAME
              value: "data-access-mcp"
            - name: SERVICE_PORT
              value: "9000"
            - name: COSMOS_DB_KEY
              valueFrom:
                secretKeyRef:
                  name: agent-mesh-secrets
                  key: cosmos-db-key
            - name: STORAGE_ACCOUNT_KEY
              valueFrom:
                secretKeyRef:
                  name: agent-mesh-secrets
                  key: storage-account-key
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 10
      nodeSelector:
        nodepool: workload
      tolerations: []
---
apiVersion: v1
kind: Service
metadata:
  name: data-access-mcp
  namespace: agent-mesh
  labels:
    app: agent-mesh
    component: data-access-mcp
    tier: mcp-server
spec:
  type: ClusterIP
  selector:
    app: agent-mesh
    component: data-access-mcp
  ports:
    - port: 9000
      targetPort: http
      protocol: TCP
      name: http
