---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: eval-guards-mcp
  namespace: agent-mesh
  labels:
    app: agent-mesh
    component: eval-guards-mcp
    tier: mcp-server
    version: "1.0.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-mesh
      component: eval-guards-mcp
  template:
    metadata:
      labels:
        app: agent-mesh
        component: eval-guards-mcp
        tier: mcp-server
        version: "1.0.0"
    spec:
      serviceAccountName: default
      containers:
        - name: eval-guards-mcp
          image: ${ACR_LOGIN_SERVER}/agent-mesh-eval-guards-mcp:latest
          ports:
            - containerPort: 9004
              name: http
              protocol: TCP
          envFrom:
            - configMapRef:
                name: agent-mesh-config
          env:
            - name: SERVICE_NAME
              value: "eval-guards-mcp"
            - name: SERVICE_PORT
              value: "9004"
            - name: AZURE_OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: agent-mesh-secrets
                  key: azure-openai-api-key
            - name: COSMOS_DB_KEY
              valueFrom:
                secretKeyRef:
                  name: agent-mesh-secrets
                  key: cosmos-db-key
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 10
      nodeSelector:
        nodepool: workload
      tolerations: []
---
apiVersion: v1
kind: Service
metadata:
  name: eval-guards-mcp
  namespace: agent-mesh
  labels:
    app: agent-mesh
    component: eval-guards-mcp
    tier: mcp-server
spec:
  type: ClusterIP
  selector:
    app: agent-mesh
    component: eval-guards-mcp
  ports:
    - port: 9004
      targetPort: http
      protocol: TCP
      name: http
